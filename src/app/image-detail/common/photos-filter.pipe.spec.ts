import { PhotosFilterPipe } from './photos-filter.pipe';

describe('PhotosFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PhotosFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
